return {
  "folke/snacks.nvim",
  opts = {
    explorer = {
      win = {
        style = "minimal",
        backdrop = false,
        border = "none",
      },
      header = {
        enabled = false,  -- Disable the header entirely if it's still problematic
      },
    },
  },
}
